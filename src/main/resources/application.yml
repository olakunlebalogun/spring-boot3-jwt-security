
spring:
  datasource:
#    url: jdbc:postgresql//jwt_db:5430/jwt_sec_db
    url: jdbc:postgresql://localhost:5430/jwt_sec_db
    username: olakunle
    password: jwt#sec
    driver-class-name: org.postgresql.Driver
  jpa:
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: create-drop
server:
  port: 54300